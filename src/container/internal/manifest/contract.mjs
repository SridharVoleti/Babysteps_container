export const manifestContract = Object.freeze({
  supportedContractVersions: Object.freeze(['1.0']),
  availableCapabilities: Object.freeze(['progress', 'audio']),
  approvedExtensionPoints: Object.freeze(['activity-renderer'])
});
